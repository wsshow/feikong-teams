package dispatch

const toolDesc = "将多个独立子任务并行分发到子智能体执行。每个子任务由一个通用子智能体独立处理。适用于多个可独立完成、无依赖关系的子任务。"

const dispatchPrompt = `

## 子任务分发

你有一个 dispatch_tasks 工具，可以将多个独立子任务并行分发到子智能体执行。

使用场景：
- 用户请求包含多个可独立完成的子任务
- 各子任务之间没有依赖关系
- 子任务不需要共享上下文或中间结果

注意事项：
- 每个子任务描述要足够详细和清晰，以便子智能体独立完成
- 子任务将以自主模式执行，工具操作自动审批（执行前会请求用户确认）

结果汇总要求：
- 执行完成后，汇总各子任务的执行结果
- 列出每个子任务执行了哪些关键操作（来自 operations 字段）
- 如有失败的子任务，说明失败原因`

const subAgentInstruction = `你是一个通用任务执行智能体。请根据任务描述独立完成工作。
- 使用可用工具高效完成任务
- 返回清晰、结构化的执行结果
- 如果遇到问题，说明原因并给出建议`
