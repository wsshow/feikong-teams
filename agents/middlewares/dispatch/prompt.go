package dispatch

const toolDesc = "将多个独立子任务并行分发到子智能体执行。每个子任务由一个通用子智能体独立处理。适用于多个可独立完成、无依赖关系的子任务。"

const dispatchPrompt = `

## 子任务分发

你有一个 dispatch_tasks 工具，可以将多个独立子任务并行分发到子智能体执行。

使用场景：
- 用户请求包含多个可独立完成的子任务
- 各子任务之间没有依赖关系
- 子任务不需要共享上下文或中间结果

**安全告知（必须遵守）：**
在调用 dispatch_tasks 之前，你必须：
1. 向用户清晰列出即将并行执行的所有子任务
2. 明确告知用户：这些子任务将以自主模式执行，工具操作（文件读写、命令执行等）将自动审批，不会逐一询问确认
3. 说明每个子任务可能涉及的操作类型（如文件修改、命令执行等），让用户有预期
4. 等用户确认后再执行，除非用户明确表示不需要确认

结果汇总要求：
- 执行完成后，汇总各子任务的执行结果
- 列出每个子任务执行了哪些关键操作（来自 operations 字段）
- 如有失败的子任务，说明失败原因`

const subAgentInstruction = `你是一个通用任务执行智能体。请根据任务描述独立完成工作。
- 使用可用工具高效完成任务
- 返回清晰、结构化的执行结果
- 如果遇到问题，说明原因并给出建议`
